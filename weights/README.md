# DigitalOrg weights

Place deployment weights here or point to them through `configs/default.yaml`.

Expected assets:

- a DigitalOrgdet text-prompt detection checkpoint
- a SAM3 image/video checkpoint
- an ROI refine checkpoint

Large model files should not be committed unless the release policy explicitly allows it.
